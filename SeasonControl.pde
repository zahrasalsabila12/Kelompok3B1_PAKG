// Scene control
static class SeasonControl {
  // Scene currently viewed by player
  static int currentSeason = 0;
  
  // If game is played for the first time
  static boolean isFirstTimePlayed = true;
}
